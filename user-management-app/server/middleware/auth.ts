import { prisma } from '../../lib/db'
import { AuthUtils } from '../../lib/auth'

export default defineEventHandler(async (event) => {
  // Middleware temporarily disabled for CSS testing
  return
})
